{% extends 'base.html.twig' %}

{% block title %}ProductOfferContent{% endblock %}

{% block body %}
    <h1>ProductOfferContent</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ product_offer_content.id }}</td>
        </tr>
        <tr>
            <th>Quantity</th>
            <td>{{ product_offer_content.quantity }}</td>
        </tr>

        </tbody>
    </table>

    <a href="{{ path('product_offer_content_index') }}">back to list</a>

    <a href="{{ path('product_offer_content_edit', {'id': product_offer_content.id}) }}">edit</a>

    {{ include('offer/content/_delete_form.html.twig') }}
{% endblock %}